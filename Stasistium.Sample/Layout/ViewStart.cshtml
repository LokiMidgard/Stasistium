@using Stasistium.Sample
@model Stasistium.Documents.MetadataContainer;
@{
    var l = Model.GetValue<PageLayoutMetadata>()?.Layout;
    if (String.IsNullOrWhiteSpace(l))
    {
        l = @"base.cshtml";
    }
    Layout = l;
}